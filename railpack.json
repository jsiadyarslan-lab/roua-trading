{
  "$schema": "https://railpack.com/config/file/v1",
  "start": "sh -c 'cd /app && bunx prisma db push --schema=./apps/web/prisma/schema.prisma --skip-generate --accept-data-loss 2>/dev/null; cd apps/web && bun x next start -H 0.0.0.0'",
  "build": {
    "commands": [
      "bunx prisma generate --schema=./apps/web/prisma/schema.prisma",
      "bun run build"
    ]
  },
  "install": {
    "commands": ["bun install --frozen-lockfile"]
  }
}
