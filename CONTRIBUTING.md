# Roua Trading — Contribution Rules

> **⚠️ قواعد صارمة إلزامية لكل مطور (بما في ذلك وكلاء الذكاء الاصطناعي)**

## القاعدة الذهبية

> **أي تعديل على كود المشروع يجب أن يُسجَّل في `BUGS.md` — بدون استثناءات.**

هذا يشمل:
- ✅ إصلاح خطأ (bug fix)
- ✅ إضافة ميزة جديدة (feature)
- ✅ تحسين أداء (performance improvement)
- ✅ إعادة هيكلة (refactoring)
- ✅ تعديل بسيط (even a 1-line change)

## كيف تتبع القاعدة

### 1. قبل أي تعديل
```bash
npx tsx scripts/verify-bugs.ts
```
اعرف ما هي الأخطاء الحالية قبل البدء.

### 2. عند اكتشاف خطأ جديد
أضف إدخالاً في `BUGS.md` بالقالب:
```markdown
### BUG-NNN: عنوان مختصر
- **Status:** OPEN
- **Severity:** CRITICAL | HIGH | MEDIUM | LOW
- **File:** `apps/web/src/path/to/file.ts:LINE`
- **Pattern (OPEN):** regex-يطابق-الكود-الخاطئ
- **Description:** ما الخاطئ
- **Impact:** ما الذي ينكسر للمستخدم
- **Fix:** (يُملأ عند الإصلاح)
- **Commit:** (يُملأ عند النشر)
- **Test:** `apps/web/src/lib/charts/__tests__/BUG-NNN.name.spec.ts` (يُملأ عند إضافة اختبار)
```

### 3. عند إصلاح خطأ
1. غيّر `**Status:**` من `OPEN` → `FIXED` في `BUGS.md`
2. أضف `**Pattern (FIXED):**` (regex يطابق الكود المُصلَح)
3. أضف `**Commit:**` (hash الـ commit)
4. أنشئ اختبار انحدار: `apps/web/src/lib/charts/__tests__/BUG-NNN.name.spec.ts`
5. شغّل: `npx tsx scripts/verify-bugs.ts` — يجب أن يظهر الخطأ كـ 🟢 FIXED
6. شغّل: `npx tsx scripts/run-regression-tests.ts` — يجب أن تجتاز كل الاختبارات

### 4. عند إضافة ميزة جديدة (ليست إصلاح خطأ)
إذا كان التعديل ميزة جديدة وليس إصلاحاً:
1. لا تحتاج لإضافة BUG-NNN
2. **ولكن** إذا الميزة تضيف كوداً جديداً قد يحتوي على أخطاء، سجّل أي مخاطر معروفة كـ `BUG-NNN` بـ status `OPEN`

## التحقق الآلي

### محلياً (قبل كل commit)
```bash
# 1. تحقق من حالة كل الأخطاء
npx tsx scripts/verify-bugs.ts

# 2. شغّل اختبارات الانحدار
npx tsx scripts/run-regression-tests.ts

# 3. إذا كل شيء أخضر، يمكنك الـ commit
```

### في CI (GitHub Actions)
كل PR يُلامس كود الشارت يُفحص تلقائياً:
1. `verify-bugs.ts` — يفشل إذا أي خطأ FIXED انتكس
2. `run-regression-tests.ts` — يفشل إذا أي اختبار فشل
3. `check-registry.ts` — يفشل إذا تم تعديل ملفات .ts/.tsx بدون تحديث `BUGS.md`

### من المتصفح (الإنتاج)
```
https://roua-trading-production.up.railway.app/dashboard/chart-bugs
```

## جدول الحالات

| الحالة | المعنى | الإجراء |
|--------|--------|---------|
| 🟢 FIXED | الإصلاح في مكانه، نمط FIXED يطابق | لا شيء — يراقبه CI |
| 🔴 PRESENT | الخطأ موجود، نمط OPEN يطابق | جدول إصلاحاً |
| 🚨 REGRESSED | كان FIXED لكن نمط OPEN يطابق مجدداً | **عاجل — أصلح فوراً** |
| ⚪ UNKNOWN | لا نمط يطابق | حدّث الأنماط في BUGS.md |

## لماذا هذه القاعدة موجودة

قبل هذا النظام:
- 3 تدقيقات اكتشفت ~300 خطأ، كل واحدة بدأت من الصفر
- نفس الأخطاء تُكتشف وتُنسى وتُعاد
- لا طريقة لمعرفة إذا عاد خطأ مُصلَح

بعد هذا النظام:
- كل خطأ له **ID مستقر** (`BUG-001` يعني نفس الشيء للأبد)
- `verify-bugs.ts` يفحص الكود في <2 ثانية
- CI يمنع دمج أي PR يعيد خطأً مُصلَحاً
- التقدم قابل للقياس: "20 مُصلَح، 0 انتكس"

## للذكاء الاصطناعي (وكلاء البرمجة)

عندما يطلب منك المستخدم إصلاح خطأ:
1. **أولاً**: اقرأ `BUGS.md` لمعرفة ما إذا كان الخطأ مسجَّلاً
2. **أصلح الكود**: أضف تعليق `BUG-NNN FIX` بجانب الإصلاح
3. **حدّث BUGS.md**: غيّر Status إلى FIXED، أضف Pattern (FIXED) و Commit
4. **شغّل verify-bugs.ts**: أكد أن الخطأ يظهر كـ FIXED
5. **شغّل run-regression-tests.ts**: أكد أن كل الاختبارات تجتاز

عندما تكتشف خطأً جديداً أثناء العمل:
1. **سجّله فوراً** في `BUGS.md` كـ OPEN
2. **لا تنتظر** حتى نهاية المهمة — سجّله أولاً ثم تابع
3. إذا أصلحته في نفس الجلسة، حدّث الحالة إلى FIXED
