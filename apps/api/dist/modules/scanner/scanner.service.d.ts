import { MarketDataAggregatorService } from '../analytics/aggregator.service';
import { TechnicalIndicatorService } from '../analytics/indicators.service';
import { AIOrchestratorService } from '../ai/services/ai-orchestrator.service';
import { RedisService } from '../../common/redis/redis.service';
import { MarketCategory, HeatmapItemDto, MultiTfResultDto, DeepAnalysisDto, ScannerScanResponseDto, MarketOverviewDto } from './scanner.types';
export declare class ScannerService {
    private readonly aggregator;
    private readonly indicators;
    private readonly aiOrchestrator;
    private readonly redis;
    private readonly logger;
    private readonly SCANNER_AI_DAILY_COST_CAP_USD;
    private readonly REDIS_SCANNER_AI_COST_KEY;
    private readonly REDIS_SCANNER_AI_COST_DATE_KEY;
    constructor(aggregator: MarketDataAggregatorService, indicators: TechnicalIndicatorService, aiOrchestrator: AIOrchestratorService, redis: RedisService);
    invalidateCache(timeframe?: string, category?: MarketCategory): Promise<void>;
    fullScan(timeframe?: string, category?: MarketCategory): Promise<ScannerScanResponseDto>;
    heatmapData(category?: MarketCategory): Promise<HeatmapItemDto[]>;
    multiTimeframeAnalysis(symbol: string): Promise<MultiTfResultDto>;
    deepAnalysis(symbol: string): Promise<DeepAnalysisDto>;
    marketOverview(): Promise<MarketOverviewDto>;
    private _stochastic;
    private _adx;
    private _vwap;
    private _supportResistance;
    private _detectPatterns;
    private _ichimoku;
    private _obv;
    private _cci;
    private _parabolicSar;
    private _fibonacci;
    private _detectDivergence;
    private _volumeProfile;
    private _detectCandlePatterns;
    private _calculateSmartScore;
    private _scanSymbol;
    private _quickScore;
    private _classifyDirection;
    private _scoreToDirection;
    private _classifySignalClass;
    private _calculateConfidence;
    private _scoreToAlignment;
    private _translateAlignment;
    private _generateExecutionHintAr;
    private _getScannerAIDailyCost;
    private _addScannerAICost;
}
