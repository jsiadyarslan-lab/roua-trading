# تقرير فحص منصة رؤى ROUA
## تقرير تدقيق الجودة والأمان والأداء
**التاريخ:** 27 أبريل 2026 | **الإصدار:** 1.0 | **المُنفذ:** Z.ai — فريق ضمان الجودة

---

## الملخص التنفيذي

تم إجراء فحص شامل لمنصة "رؤى ROUA" للتداول، وهي منصة ويب متكاملة تعتمد على بنية Monorepo تتضمن Next.js 16 كواجهة أمامية وNestJS كخلفية، مع PostgreSQL وPrisma ORM وRedis وRabbitMQ. المنصة تحتوي على 56 مسار API، أكثر من 20 صفحة ومكون React، و6 نماذج ذكاء اصطناعي متكاملة.

**النتيجة العامة:** المنصة تمتلك بنية تقنية قوية وميزات متقدمة، لكنها تعاني من ثغرات أمنية حرجة تحتاج إصلاحًا فوريًا قبل أي إطلاق تجريبي، أبرزها: تعطيل المصادقة بشكل افتراضي، نقطة تصحيح الأخطاء تكشف بيانات حساسة، غياب حماية CSRF، وغياب رؤوس الأمان.

---

## بطاقة التقييم الشامل

| البعد | التقييم | حرجة | متوسطة | طفيفة |
|-------|---------|------|--------|-------|
| الخلفية — المسارات والتحكم | ⚠️ يحتاج تحسين | 3 | 5 | 4 |
| الواجهة — المكونات والصفحات | ⚠️ يحتاج تحسين | 2 | 5 | 6 |
| تكامل الذكاء الاصطناعي | ⚠️ يحتاج تحسين | 1 | 4 | 2 |
| قاعدة البيانات | ⚠️ يحتاج تحسين | 2 | 4 | 3 |
| الأمان | 🔴 حرج | 5 | 3 | 2 |
| الأداء | ⚠️ يحتاج تحسين | 1 | 4 | 3 |
| **الإجمالي** | **🔴 يحتاج إصلاح حرج** | **14** | **25** | **20** |

---

## جدول حالة الميزات

| الميزة | الحالة | ملاحظات |
|--------|--------|---------|
| المصادقة (WebAuthn + Google OAuth) | 🔴 معطلة افتراضيًا | ENABLE_AUTH يجب تعيينه إلى 1 يدويًا |
| لوحة التحكم الرئيسية | ✅ تعمل | عرض بيانات سوق حية، شموع، مراكز |
| المحفظة والصفقات | ⚠️ تعمل جزئيًا | AI Coach يحتاج إصلاح مسارات API داخلية |
| Neural Lab — Backtest | ⚠️ تعمل جزئيًا | يعمل مع بيانات محاكاة عند غياب NestJS |
| Neural Lab — Optimizer | ⚠️ تعمل جزئيًا | يُحسّن محليًا كاحتياط |
| Neural Lab — Comparison | ⚠️ تعمل جزئيًا | مقارنة استراتيجيتين مع رسم بياني شريطي |
| TradeChart (شموع + دخول/خروج) | ✅ تعمل | يستخدم lightweight-charts مع تحميل ديناميكي |
| تصدير التقارير (PDF/Excel/CSV/JSON) | ✅ تعمل | تنزيل ملفات حقيقية مع pdfkit وexceljs |
| الأخبار متعددة اللغات | ⚠️ تعمل جزئيًا | ترجمة AI تعمل، تحليل مشاعر يحتاج إصلاح تحليل XML |
| AI Council (6 نماذج) | 🔴 غير متصل من الواجهة | مسار /api/ai/consensus-nest غير موجود |
| AI Coach في المحفظة | 🔴 مكسور | يستدعي /api/trading/trades الذي لا وجود له |
| التداول عبر Alpaca | 🔴 خطر أمني | يعمل لكن بدون أي مصادقة |
| الماسح الضوئي (Scanner) | ✅ تعمل | مسح سوقي متعدد مع تحليل تقني |
| الإشارات (Signals) | ✅ تعمل | إشارات ذكية مع مستوى ثقة |
| التقويم الاقتصادي | ℹ️ بيانات ثابتة | أحداث ثابتة وليست بيانات حية |
| نقطة تصحيح الأخطاء (/api/auth/debug) | 🔴 خطر أمني | تكشف متغيرات بيئة حساسة بدون مصادقة |
| Swarm (أسراب العقول) | ✅ تعمل | تنسيق بين عدة وكلاء AI |
| استراتيجيات متقدمة | ✅ تعمل | تقارير مؤسسية مع مصفوفة مخاطر |

---

## المرحلة 1: فحص الخلفية (Backend Audit)

### نظرة عامة
- **56 ملف مسار API** موزعة على: auth (7)، ai (5)، neural (12)، news (4)، coach (3)، scanner (8)، signals (4)، exchange (3)، alpaca (5)، وغيرها
- معالجة الأخطاء (try-catch): **96%** ✅
- المصادقة على المسارات: **25% فقط** ❌
- التحقق من المدخلات: **36% فقط** ❌
- استدعاءات API خارجية: **63%**

### المشاكل الحرجة

1. **المصادقة معطلة افتراضيًا:** الوسيطة (middleware) لا تفعل شيئًا ما لم يُعيَّن `ENABLE_AUTH=1`. جميع مسارات لوحة التحكم وAPI متاحة بدون تسجيل دخول.
2. **نقطة التصحيح تكشف أسرارًا:** `/api/auth/debug` يعرض أجزاء من NEXTAUTH_SECRET وGOOGLE_CLIENT_SECRET وDATABASE_URL بدون مصادقة.
3. **مسارات Alpaca بدون مصادقة:** تنفيذ صفقات حقيقية وإغلاق مراكز وعرض رصيد الحساب بدون أي تحقق.
4. **Coach يستدعي مسارات غير موجودة:** `/api/coach/ask` و`/api/coach/performance` يستدعيان `/api/trading/trades` الذي لا وجود له.
5. **userId ثابت في chart-preference:** يستخدم `userId = 'default-user'` بدلاً من الجلسة.
6. **غياب Rate Limiting:** لا تقييد على عدد الطلبات.

### نقاط إيجابية
- أنماط تدهور لطيف ممتازة (NestJS → local fallback → simulated data)
- مصادر بيانات متعددة مع سلاسل احتياطي (5-7 مصادر للأسعار)
- تخزين مؤقت في الذاكرة مع TTL وصلاحية البيانات القديمة
- Circuit breaker لـ TwelveData
- تسجيل تدقيق في مسارات المصادقة

---

## المرحلة 2: فحص الواجهة الأمامية (Frontend Audit)

### مشاكل رئيسية

1. **صفحات غير متجاوبة مع الهواتف:** AI Center (3 أعمدة ثابتة)، الاستراتيجيات (شريط جانبي 260px)، الماسح (شريط جانبي ثابت). مكسورة تمامًا على الهاتف.
2. **13+ عبارة console.log متبقية** في ملفات trading/page.tsx، portfolio/page.tsx، BacktestPanel.tsx، وعدة hooks.
3. **إمكانية الوصول (Accessibility) ضعيفة:** نوافذ حوار بدون `role="dialog"`، تبويبات بدون `role="tablist"`، مفاتيح تبديل بدون `role="switch"`، جداول بدون `<caption>`، مدخلات بدون `aria-label`.
4. **300+ سلسلة عربية مدمجة في الكود:** لا يوجد إطار عمل i18n. صفحة الإعدادات تعرض "العربية (AR)" كقراءة فقط.
5. **80% من المكونات تستخدم أنماط مضمّنة** بدل Tailwind. صفحة لوحة التحكم تحتوي على ~400 سطر CSS داخل `<style>`.
6. **أخطاء مبتلعة بصمت:** صفحة الأخبار تعرض حالة فارغة بدل رسالة خطأ. صفحة AI لديها catch blocks فارغة.

---

## المرحلة 3: فحص تكامل الذكاء الاصطناعي

### النماذج الستة

| النموذج | المزود | المهلة | أقصى رموز | معالجة الأخطاء |
|---------|--------|--------|-----------|----------------|
| Llama 3.3 70B | Groq | 30 ث | 1024 | ✅ stub (ثقة 0) |
| Gemini 2.0 Flash | Google | 60 ث | 2048 | ✅ stub (ثقة 0) |
| GLM-4 | Zhipu AI | 60 ث | 2048 | ✅ stub (ثقة 0) |
| Mistral-7B/Phi-3/Llama-3 | HuggingFace | 60 ث | 1024 | ✅ ثلاثي + stub |
| Qwen2.5 | Ollama (محلي) | 120 ث | 1024 | ✅ stub (ثقة 0) |
| Claude 3.5 Sonnet | AWS Bedrock | 60 ث | 2048 | ✅ stub (ثقة 0) |

### مشاكل حرجة في الذكاء الاصطناعي

1. **AI Council غير متصل:** مسار `/api/ai/consensus/route.ts` يستدعي `/api/ai/consensus-nest` غير الموجود. المجلس الحقيقي لا يُستدعى أبدًا من الواجهة.
2. **استخراج التصويت هش:** البحث عن كلمات مفتاحية يُفسّر "لا أنصح بالشراء" كـ "شراء".
3. **قيم الثقة ثابتة:** كل نموذج يُرجع قيمة ثابتة بغض النظر عن جودة المخرجات.
4. **7 استدعاءات AI لكل إجماع:** 6 نماذج + 1 استراتيجية رئيسية = مكلف جدًا. مع الجدولة كل 15 دقيقة لـ 5 رموز = 140 استدعاعة/ساعة.
5. **لا تخزين مؤقت لاستجابات AI:** استعلامات متطابقة تضرب جميع النماذج في كل مرة.

---

## المرحلة 4: فحص قاعدة البيانات

### الفهارس المفقودة

| النموذج | المشكلة | الخطورة |
|---------|---------|---------|
| PortfolioAsset | لا فهرس على portfolioId (FK) | 🔴 حرج |
| NewsArticle | لا قيد فريد على url | ⚠️ متوسطة |
| PortfolioAsset | لا قيد فريد على (portfolioId, symbol) | ⚠️ متوسطة |
| Order/Position | لا فهرس على FK (exchangeCredentialId, credentialId) | ⚠️ متوسطة |

### مشاكل تخزين البيانات الحساسة

1. **رموز OAuth مخزنة بنص صريح** في نموذج Account ❌
2. **Float بدل Decimal** في حقول مالية (Position، PortfolioAsset) ❌
3. **علاقات Prisma مفقودة** لعدة حقول FK (orderId، positionId، credentialId، signalId)
4. **تشفير غير متسق:** ExchangeCredential يستخدم AES-256-GCM مع authTag، ApiKey بدونه

### حقول مفقودة لمنصة تداول
- timeInForce (GTC, IOC, FOK)
- رافعة مالية وسعر تصفية
- نموذج إشعارات/تنبيهات
- تسجيل استهلاك AI (رموز، تكلفة، زمن)
- إعدادات المخاطرة لكل مستخدم
- نتائج Backtest مخزنة

---

## المرحلة 5: فحص الأمان

### ثغرات حرجة (5)

1. **CSRF مفقود:** `sameSite: 'none'` في الإنتاج بدون رموز CSRF
2. **غياب رؤوس الأمان:** لا Content-Security-Policy، X-Frame-Options، HSTS، إلخ
3. **أسرار مُلتزمة في Git:** ملف .env بكلمات مرور حقيقية في التاريخ
4. **مفتاح تشفير احتياطي:** ملح ثابت 'roua-salt' + NEXTAUTH_SECRET كأساس
5. **NEXTAUTH_SECRET حتمي:** يُولَّد من تجزئة SHA-256 لمتغيرات بيئة أخرى

### ثغرات متوسطة (3)
- `ignoreBuildErrors: true` في Next.js
- تحويل كامل رؤوس الطلبات إلى NestJS
- مدخلات المستخدم في عناوين URL بدون تحقق كامل

### نقاط إيجابية
- ✅ لا استعلامات SQL خام غير معلمة
- ✅ لا ثغرات XSS (استخدام آمن لـ dangerouslySetInnerHTML)
- ✅ CORS مُعد بشكل صحيح
- ✅ مفاتيح API لا تُرسل للعميل
- ✅ WebAuthn مُنفذ بشكل صحيح

---

## المرحلة 6: فحص الأداء

### حجم الحزمة
- recharts (400+ KB) وframer-motion (150+ KB) بدون تقسيم كود
- فقط 3 مكونات تستخدم تحميل ديناميكي من أصل 30+
- pdfkit وexceljs محملان ديناميكيًا ✅

### التخزين المؤقت
- أخبار: 5 دقائق ✅
- أسعار: 5-30 ثانية حسب المزود ✅
- ماسح: 60 ثانية ✅
- Redis مع نمط cacheOrGet ✅
- لا تخزين مؤقت لاستجابات AI ❌

### مشاكل الأداء
1. **استعلامات N+1** في trading.service.ts
2. **لا تجمع اتصالات DB** (بدون PgBouncer)
3. **ماسح الأسعار:** 24 طلب HTTP متزامن
4. **لا next/image** في المشروع بالكامل
5. **مكونات ضخمة** بدون تقسيم (500+ سطر)

---

## قائمة الأخطاء حسب الخطورة

### حرجة (14)

| # | المشكلة | المجال | التأثير |
|---|---------|--------|---------|
| C1 | المصادقة معطلة افتراضيًا | أمان | كل المسارات مفتوحة |
| C2 | /api/auth/debug يكشف أسرارًا | أمان | تسريب بيانات حساسة |
| C3 | مسارات Alpaca بدون مصادقة | أمان/تداول | صفقات غير مصرح |
| C4 | sameSite: 'none' بدون CSRF | أمان | طلبات مزورة |
| C5 | غياب رؤوس الأمان | أمان | clickjacking |
| C6 | .env مُلتزَم في Git | أمان | بيانات مكشوفة |
| C7 | مفتاح تشفير احتياطي + ملح ثابت | أمان | فك تشفير محتمل |
| C8 | NEXTAUTH_SECRET حتمي | أمان | JWT قابل للتوقع |
| C9 | رموز OAuth بنص صريح | قاعدة بيانات | تسريب عند اختراق |
| C10 | AI Council غير متصل | AI/Backend | المجلس لا يُستدعى |
| C11 | Coach يستدعي مسارات غير موجودة | Backend | AI Coach مكسور |
| C12 | Float بدل Decimal | قاعدة بيانات | فقدان دقة مالية |
| C13 | لا فهرس على PortfolioAsset.portfolioId | قاعدة بيانات | JOIN بطيء |
| C14 | استخراج تصويت AI هش | AI | قرارات تداول خاطئة |

### متوسطة (25)
M1-M25: تشمل غياب Rate Limiting، userId ثابت، ignoreBuildErrors، console.log متبقية، صفحات غير متجاوبة، 300+ سلسلة بدون i18n، أكسسبيليتي ضعيفة، أخطاء مبتلعة، قيم ثقة AI ثابتة، 7 استدعاءات AI لكل إجماع، علاقات Prisma مفقودة، أنماط مضمّنة بدل Tailwind، وغيرها.

### طفيفة (20)
تشمل: حقول مفقودة (timeInForce، رافعة مالية)، بيانات ثابتة للتقويم، لا next/image، مكونات ضخمة، كائنات Theme مكررة، وغيرها.

---

## التوصيات

### إجراءات فورية (قبل أي إطلاق)

1. **تفعيل المصادقة:** اعكس منطق الوسيطة — مفعلة افتراضيًا، تعطيل اختياري في التطوير فقط
2. **حذف /api/auth/debug:** أو إضافة مصادحة إدارية + حظر في الإنتاج
3. **حماية مسارات Alpaca:** إضافة مصادقة + تسجيل تدقيق
4. **إصلاح CSRF:** تغيير sameSite إلى 'lax' في الإنتاج
5. **إضافة رؤوس الأمان:** X-Frame-Options، CSP، HSTS، X-Content-Type-Options
6. **تدوير الأسرار:** تغيير جميع كلمات المرور + تنظيف Git history
7. **إصلاح AI Council:** تصحيح مسار consensus-nest
8. **إصلاح AI Coach:** تصحيح المسارات الداخلية

### قبل الإنتاج

- إضافة Rate Limiting (Redis INCR + EXPIRE)
- تشفير رموز OAuth قبل التخزين
- تحويل Float إلى Decimal في البيانات المالية
- إضافة الفهارس المفقودة
- تحسين استخراج تصويت AI (JSON مهيكل)

### تحسينات مستقبلية

- تقسيم الكود (Code Splitting) للصفحات الثقيلة
- تخزين مؤقت لاستجابات AI بـ Redis
- إطار عمل i18n (next-intl)
- إمكانية الوصول (ARIA roles)
- PgBouncer لتجمع اتصالات DB
- استجابة الهاتف للصفحات المكسورة

---

## ملخص الجاهزية للإطلاق التجريبي

### 🔴 المنصة ليست جاهزة للإطلاق التجريبي بصيغتها الحالية
**يُقدَّر زمن الإصلاح:** 2-3 أسابيع لمعالجة المشاكل الحرجة والمتوسطة

### خارطة طريق مقترحة

| الأسبوع | المهام | الأولوية |
|---------|--------|----------|
| 1 | إصلاح المصادقة، حذف debug، حماية Alpaca، CSRF + رؤوس أمان، تدوير الأسرار | 🔴 حرج |
| 2 | إصلاح AI Council + Coach، Rate Limiting، تشفير OAuth، Float→Decimal | 🔴 حرج |
| 3 | استجابة الهاتف، إزالة console.log، فهارس، تحسين تصويت AI، Error Boundaries | ⚠️ متوسطة |
| 4+ | i18n، Code Splitting، تخزين AI مؤقت، PgBouncer، Accessibility | ℹ️ تحسينية |

### نقاط القوة
- بنية تقنية متقدمة (Next.js 16 + NestJS + Prisma + Redis)
- معالجة أخطاء ممتازة (96% تغطية)
- تكامل AI شامل مع 6 نماذج وسلاسل احتياطي
- مصادقة WebAuthn (Passkeys)
- تشفير API Keys بـ AES-256-GCM
- تصميم داكن احترافي
- تخزين مؤقت جيد مع Redis
- أنماط تدهور لطيف

**الخلاصة:** منصة "رؤى ROUA" تمتلك أساسًا تقنيًا قويًا وميزات متقدمة. الثغرات الحالية هي في الغالب إعدادات أمنية وتكاملية وليست عيوبًا هيكلية. بعد معالجة المشاكل الحرجة (أسبوعان)، ستكون المنصة جاهزة للإطلاق التجريبي المحدود. الإطلاق العام يتطلب 3-4 أسابيع.
