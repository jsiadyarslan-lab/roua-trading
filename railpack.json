{
  "$schema": "https://railpack.com/config/file/v1",
  "start": "bash start.sh",
  "build": {
    "commands": [
      "npx prisma generate --schema=./prisma/schema.prisma",
      "npm run build"
    ]
  },
  "install": {
    "commands": ["npm ci --workspaces --include-workspace-root"]
  }
}
