import { ConfigService } from '@nestjs/config';
import { RedisService } from '../../../common/redis/redis.service';
import { GlmService } from '../../../modules/ai/services/glm.service';
import { ContentGenerationRequest, GeneratedContent } from '../types/content.types';
export declare class ContentGeneratorService {
    private readonly configService;
    private readonly redis;
    private readonly glmService;
    private readonly logger;
    private readonly DAILY_QUOTA;
    private readonly DRAFT_CACHE_TTL;
    private readonly DEFAULT_AI_CONFIG;
    constructor(configService: ConfigService, redis: RedisService, glmService: GlmService);
    generate(request: ContentGenerationRequest): Promise<GeneratedContent>;
    generateBreakingAlert(topic: string, symbols: string[], context: string): Promise<GeneratedContent>;
    private _generateWithAI;
    private _buildSystemPrompt;
    private _buildUserPrompt;
    private _generateSeoMetadata;
    private _generateExcerpt;
    private _assessQuality;
    private _calculateConfidence;
    private _assessSentiment;
    private _assessImpactLevel;
    private _generateRiskWarnings;
    private _countWords;
    private _getCategoryArabic;
    private _extractTags;
    private _cacheDraft;
}
