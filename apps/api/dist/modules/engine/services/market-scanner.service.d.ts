import { PrismaService } from '../../../common/prisma/prisma.service';
import { RedisService } from '../../../common/redis/redis.service';
import { SignalGeneratorService } from '../../analytics/signal-generator.service';
import { AnalyticalAIService } from '../../analytics/analytical-ai.service';
import { MarketDataAggregatorService } from '../../analytics/aggregator.service';
import { AuditService } from '../../../audit/audit.service';
export declare class MarketScannerService {
    private readonly prisma;
    private readonly redis;
    private readonly signalGenerator;
    private readonly analyticalAI;
    private readonly aggregator;
    private readonly audit;
    private readonly logger;
    private readonly MIN_CONFIDENCE;
    private readonly MIN_TECH_SCORE;
    private readonly DEFAULT_SYMBOLS;
    private isScanning;
    private readonly SCANNER_DAILY_COST_CAP_USD;
    private readonly REDIS_SCANNER_COST_KEY;
    private readonly REDIS_SCANNER_COST_DATE_KEY;
    constructor(prisma: PrismaService, redis: RedisService, signalGenerator: SignalGeneratorService, analyticalAI: AnalyticalAIService, aggregator: MarketDataAggregatorService, audit: AuditService);
    runMarketScan(): Promise<void>;
    forceScan(userId: string, symbols?: string[]): Promise<any>;
    getLastScan(): Promise<any>;
    private _collectSymbols;
    private _processBatch;
    private _getSystemUser;
    private _sleep;
    private _getScannerDailyCost;
    private _addScannerCost;
}
